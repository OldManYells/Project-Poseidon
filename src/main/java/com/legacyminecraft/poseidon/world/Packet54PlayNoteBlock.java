package com.legacyminecraft.poseidon.world;

/**
 * World-local note block packet scaffold.
 */
public class Packet54PlayNoteBlock extends Packet {
    public Packet54PlayNoteBlock(int x, int y, int z, int instrument, int note) {
    }
}
